import React from 'react';

function HelpButton({ handleHelpButton }) {
    return (
        <div className="help-button-container">
            <button className="help-button">?</button>
        </div>
    );
}

export default HelpButton;